name: UW Groups
type: module
description: Implements a UW groups -> Drupal roles mapping via the UW Groups Web Service (through LTS)
core: 8.x
configure: uw_groups.admin
package: UW iSchool
dependencies:
  - user
  - lts